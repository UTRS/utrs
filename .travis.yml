language: php

php:
  - 5.5
  - 5.4
  - 5.3
  
sudo: false

git:
  submodules: true

script:
  - phpunit

notifications:
  irc:
    channels:
      - "chat.freenode.net#wikipedia-en-utrs-dev"
    on_success: change
    on_failure: always
    template:
      - "%{repository}/%{branch}/%{commit} - %{author} %{message} %{build_url}"